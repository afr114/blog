<%= form_for [:admin, @post] do |q| %>
  <%= q.text_field :title, placeholder: "Title" %> <br><br>
  <%= q.label :content %> <br>
  <%= q.text_area :content, size: "120x20"%> <br><br>
  <%= q.submit %>
<% end %>
